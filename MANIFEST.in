# Source distributions include only installable package code + build metadata.
# Excludes wiki/, .cursor/, .claude/, tests at repo root, etc.

include pyproject.toml
include README.md
graft src
global-exclude **/__pycache__
global-exclude **/*.py[cod]
global-exclude **/*~
